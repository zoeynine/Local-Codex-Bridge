{
  "name": "local-codex-bridge",
  "version": "2.1.2",
  "private": true,
  "description": "Local stdio MCP bridge for native Codex sessions.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test dist/test/runtime.test.js dist/test/app-server.test.js dist/test/tools.test.js dist/test/mcp.test.js dist/test/checkpoint.test.js dist/test/shutdown.test.js dist/test/launcher.test.js",
    "start": "node dist/src/index.js",
    "smoke:live": "node scripts/live-smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
